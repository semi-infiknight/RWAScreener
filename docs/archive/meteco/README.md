# Meteco archive (moved into RWAScreener)

Cursor chats from the `~/Vibecode/meteco` workspace, plus Grok CLI transcripts and the old project brain.

**Read first:** `docs/GROK-ORIGIN.md` (compressed). This folder is the raw dump.

- **This folder (git):** redacted JSONL + operator prompts + grok markdown. Safe for GitHub.
- **`archive/meteco-private/` (gitignored):** full unredacted copy including `.env`, grok CLI sessions (~20MB), and Cursor `workspaceStorage`.

Cursor’s chat sidebar is still bound to the meteco workspace id. These files are the durable copy.

Conversation: [Grok CLI pause location](2e685511-2ad3-4f82-9f9d-a9478e0fc190)
